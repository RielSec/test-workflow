name: 'test acitions'
description: 'test of another action' 
author: 'Arnaelcio Gomes Pereira'

inputs:
  GITHUB_TOKEN:
    description: 'GitHub Token' 
    required: true
  TENOR_TOKEN:
    description: 'Tenor API Token' 
    required: true

runs:
  using: 'node12'
  main: 'dist/index.js'
